# Páginas

As rotas estão declaradas em `src/router/index.js`. As telas públicas ficam em `src/views/portal`; as telas da equipe ficam em `src/views/admin`. Não há geração automática de rotas por nome de arquivo.
